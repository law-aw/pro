# fly.toml app configuration file generated for notices on 2026-07-26T02:04:11Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'notices'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  DATA_DIR = '/data'
  HOST = '0.0.0.0'
  PORT = '3000'
  ROLE = 'hub'

[[mounts]]
  source = 'notice_data'
  destination = '/data'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
